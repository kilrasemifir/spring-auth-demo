server:
  port: ${SERVER_PORT:8080}

spring:  data:    mongodb:      host: ${MONGO_HOST:localhost}
      port: ${MONGO_PORT:27017}  elasticsearch:
        rest:
      uris:
      - http://${ELASTICSEARCH_HOST:localhost}:9200      password: changeme
      username: elastic
